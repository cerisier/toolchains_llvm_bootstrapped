load("@rules_cc//cc:cc_library.bzl", "cc_library")

cc_library(
    name = "libc_headers",
    includes = [
        ".",
    ],
    hdrs = glob([
        "hdr/*.h",
        "include/**/*.h",
        "shared/*.h",
        "src/**/*.h",
    ]),
    deps = select({
        "@toolchains_llvm_bootstrapped//config:msan_enabled": ["@compiler-rt//:msan_interface"],
        "//conditions:default": [],
    }),
    visibility = ["//visibility:public"],
)
